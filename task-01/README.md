# Task-01: Калькулятор с функциями

## 📌 Условие
Создать модуль калькулятора, реализующий базовые арифметические операции с использованием функций.

## ✅ Что реализовано

- `add(a, b)` — сложение
- `subtract(a, b)` — вычитание
- `multiply(a, b)` — умножение
- `divide(a, b)` — деление с проверкой деления на 0
- `isValidNumber(value)` — проверка на число
- `formatResult(result)` — округление результата до двух знаков
- `calculate(operation, a, b)` — универсальная функция, вызывающая нужную операцию

## 🛠️ Использовано

- `function declaration` и `arrow functions`
- Условные конструкции `if / switch`
- Валидация данных
- Модульный и читаемый код

## 💡 Примеры

```js
calculate('add', 5, 3);      // 8
calculate('divide', 10, 3);  // 3.33
calculate('divide', 10, 0);  // "Ошибка: деление на ноль"
